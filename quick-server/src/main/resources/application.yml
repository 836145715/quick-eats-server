server:
  port: 8081

spring:
  application:
    name: quick-eats
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://8.137.56.88:3306/quick_eats?serverTimezone=Asia/Shanghai&useUnicode=true&characterEncoding=utf-8&zeroDateTimeBehavior=convertToNull&useSSL=false&allowPublicKeyRetrieval=true
    username: root
    password: 836145715
  jackson:
    date-format: yyyy-MM-dd HH:mm:ss
    time-zone: GMT+8
    serialization:
      write-dates-as-timestamps: false
  servlet:
    multipart:
      enabled: true
      max-file-size: 10MB
      max-request-size: 10MB
  data:
    redis:
      host: 8.137.56.88
      port: 16379
      password: 836145715
      timeout: 3000ms

# MyBatis-Plus配置
mybatis-plus:
  configuration:
    # 开启驼峰命名
    map-underscore-to-camel-case: true
    # 打印SQL日志
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
  global-config:
    db-config:
      # 主键生成策略
      id-type: ASSIGN_ID
      # 逻辑删除字段
      logic-delete-field: isDeleted
      logic-delete-value: 1
      logic-not-delete-value: 0
  # XML映射文件位置
  mapper-locations: classpath*:/mapper/**/*.xml

# JWT配置
jwt:
  admin:
    # JWT加解密使用的密钥 这里类中自动生成了密钥
    secret: f4e2e52034348f86b67cde581c0f9eb5
    # token有效时长，单位分钟
    expire: 1440
    # header名称
    header: Authorization
    # token前缀
    tokenPrefix: Bearer

  user:
    # JWT加解密使用的密钥 这里类中自动生成了密钥
    secret: f4e2e52034348f86b67cde581c0f9ec6
    # token有效时长，单位分钟
    expire: 1440
    # header名称
    header: Authorization
    # token前缀
    tokenPrefix: Bearer

# 日志配置
logging:
  level:
    com.zmx.quickserver: debug
    org.springframework: warn

minio:
  url: http://8.137.56.88:9000 #ip地址
  access-key: 836145715 #  账号
  secret-key: zxc147258 #  密码
  secure: false #如果是true，则用的是https而不是http,默认值是true
  bucket-name: "quick-eats" # 桶的名字 相当于文件夹


wechat:
  appid: wx57a7fc716760909e
  secret: 6ac060c1a86cdd167b5ce931a3057df0
